import time

from logzero import logger

import settings
from app import app


if __name__ == "__main__":
    config = settings.load_config()

    logger.info(f"Server running in mode {config.__name__.lower()} and port {config.PORT}")

    time.sleep(1)

    app.run(host=config.HOST, port=int(config.PORT), debug=False)
